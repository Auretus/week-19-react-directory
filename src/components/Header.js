import React from 'react';

function Header () {
  return (
    <div className="header center">
      <h1>Employee Directory</h1>
      <p>Click on buttons to sort by category, or use the search box to narrow results by first name or last name.</p>
    </div>
  )
};

export default Header;